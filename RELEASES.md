# LocalWave Release Files

Current release files belong under `releases/`.

Older release files belong under `archive/`.

Use one directory per application version and keep filenames descriptive and consistent.
